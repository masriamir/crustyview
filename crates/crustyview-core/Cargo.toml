[package]
name = "crustyview-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
crustywad = "0.9"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
# Same major as crustywad's binrw, to construct ParseError sources in tests.
binrw = "0.15"
serde_json = "1"

[lints]
workspace = true

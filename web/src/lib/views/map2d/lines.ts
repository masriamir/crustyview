/**
 * Classic-style teleport source line color (#40) — the indigo family shared
 * with the teleport things, so pads and sources read as one concept. Shared by
 * the canvas stroke and the chip swatch.
 */
export const CLASSIC_LINE_TELEPORT = '#5e5ce6';

/**
 * Classic-style sector overlay colors (#49). Secrets take a magenta clearly
 * distinct from the ML_SECRET gold; damage reads as nukage — an acid green
 * distinct from the teal-leaning player/health greens. Shared by the canvas
 * strokes and the chip swatches.
 */
export const CLASSIC_LINE_SECTOR_SECRET = '#ff4fd8';
export const CLASSIC_LINE_SECTOR_DAMAGE = '#7ddb1e';
